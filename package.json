{
  "name": "veil-ribbon-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5187 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5187 --strictPort",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "three": "0.182.0"
  },
  "devDependencies": {
    "vite": "8.0.10"
  }
}
